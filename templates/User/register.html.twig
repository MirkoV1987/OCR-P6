{# templates/User/register.html.twig #}

{% extends "base.html.twig" %}

{% block title %}SnowTricks - Créer un compte{% endblock %}

{% block body %}

{{ include("Parts/_main_header.html.twig") }}

<!-- Page Content -->
<div class="container-fluid">
  <div class="row justify-content-center" style="margin-top: 5%;">
    <div class="col-xs-12 col-sm-8 col-md-6 col-lg-5 bg-light card mt-7 p-5">
      <h1 class="h3 mb-3 font-weight-normal text-center text-uppercase">Créer un compte</h1>
        <div class="card-body">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <div class="form-group text-center">
            <button type="submit" class="btn btn-primary"><i class="fas fa-check"></i> Je m'inscris !</button>
        </div>
        {{ form_end(form) }}
      </div>
    </div>
  </div>
</div>

<!--footer-->
  {{ include("Parts/_footer.html.twig") }}
<!--end-footer-->

{% endblock %}
